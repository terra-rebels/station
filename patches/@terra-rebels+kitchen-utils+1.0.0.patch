diff --git a/node_modules/@terra-rebels/kitchen-utils/dist/kitchen-utils.cjs.development.js b/node_modules/@terra-rebels/kitchen-utils/dist/kitchen-utils.cjs.development.js
index d428e4f..57cd868 100644
--- a/node_modules/@terra-rebels/kitchen-utils/dist/kitchen-utils.cjs.development.js
+++ b/node_modules/@terra-rebels/kitchen-utils/dist/kitchen-utils.cjs.development.js
@@ -277,7 +277,7 @@ var toAmount = function toAmount(value, config) {
 /* denom */
 
 var readDenom = function readDenom(denom) {
-  return !isDenom(denom) ? "" : isDenomLuna(denom) ? "Luna" : isDenomTerra(denom) ? denom.slice(1, 3).toUpperCase() + "T" : isDenomIBC(denom) ? denom.replace("ibc/", "") : denom.slice(1).toUpperCase();
+  return !isDenom(denom) ? "" : isDenomLuna(denom) ? "Lunc" : isDenomTerra(denom) ? denom.slice(1, 3).toUpperCase() + "T" : isDenomIBC(denom) ? denom.replace("ibc/", "") : denom.slice(1).toUpperCase();
 };
 /* percent */
 
